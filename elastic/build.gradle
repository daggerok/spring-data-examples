plugins {
  id "idea"
  id "eclipse"
}

allprojects {

  apply plugin: "base"

  version "0.0.1"
  group "daggerok"

  task wrapper(type: Wrapper) {
    gradleVersion = "4.3"
  }

  build.shouldRunAfter assemble
  assemble.shouldRunAfter clean
}
